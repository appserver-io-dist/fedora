<?xml version="1.0"?>
<!DOCTYPE project>
<project name="appserver-io-dist/fedora-tests-performance" basedir=".." default="run-performance-tests">

    <!-- ==================================================================== -->
    <!-- Runs all available performance tests                                 -->
    <!-- ==================================================================== -->
    <target name="run-performance-tests" depends="ensure-runtime-installed, ensure-dist-installed">
        <!-- test if the jmeter testplan runs through without errors -->
        <antcall target="measure-jmeter-example-testplan" />
    </target>

    <!-- ==================================================================== -->
    <!-- Tests if the jmeter testplan configured against the example app      -->
    <!-- does run without any errors                                          -->
    <!-- ==================================================================== -->
    <target name="measure-jmeter-example-testplan">
        <!-- test if the testplan runs through -->
        <exec executable="${jmeter.vagrant.basedir}/${jmeter.package.name}/bin/jmeter" failonerror="true">
            <arg line="-n -t ${jmeter.example-test.path}/example-testplan.jmx -l ${vagrant-reports.dir}/performance-example-testplan.jtl"/>
        </exec>
    </target>

    <!-- ==================================================================== -->
    <!-- Runs all available integration tests within an expected vagrant VM   -->
    <!-- ==================================================================== -->
    <target name="run-vagrant-performance-tests">
        <!-- start our vagrant box -->
        <antcall target="start-vagrant-test-box" />
        <!-- invoke original integration tests within vagrant box -->
        <exec dir="${temp.dir}" executable="vagrant" errorproperty="performance-test.failed">
            <arg line="ssh -c 'sudo ant run-performance-tests -buildfile ${vagrant.basedir}/build.xml -Dgithub.oauth.token=${github.oauth.token} -Dbuild.number=${build.number} -Dtarget-os.version=${target-os.version}'" />
        </exec>
        <!-- destroy the box after all work is done -->
        <antcall target="destroy-vagrant-box" />
        
        <!-- tell them about any failures we might encountered -->
        <fail if="performance-test.failed" message="The performance tests failed with message '${performance-test.failed}', see log above." />
    </target>

</project>
